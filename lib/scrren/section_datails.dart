import 'package:flutter/material.dart';
class SectionDatails extends StatelessWidget {
  const SectionDatails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
